## Agentic AI MCP and A2A with FastMCP - DAY 4

### Download Trainging Document

[Click here to download the training document](https://drive.google.com/drive/folders/1PgOFXacZ-tOM7auvrIPLtOwrh-MkWxay?usp=sharing)

### 📋 Content

1. [Workshop - MCP Server CSV Agent Scalable](#workshop---mcp-server-csv-agent-scalable)
2. [Deploy MCP Server to FastMCP Cloud](#deploy-mcp-server-to-fastmcp-cloud)
3. [N8N Integration with MCP Server](#n8n-integration-with-mcp-server)

### 1. Workshop - MCP Server CSV Agent (CSV Analytics) Scalable

โปรเจ็กต์นี้เป็น **MCP Server** ที่พัฒนาด้วย Python โดยใช้ library `fastmcp` เพื่อให้บริการเครื่องมือ (Tools), คำสั่ง (Prompts), และข้อมูล (Resources) สำหรับการจัดการและวิเคราะห์ไฟล์ CSV ให้กับ AI Agent (เช่น Claude Desktop หรือ Custom Client)

#### 📂 1.1 โครงสร้างโปรเจ็กต์

```
workshop4-mcp-csv-agent/
├── data/                   # โฟลเดอร์เก็บไฟล์ CSV ที่ใช้ทดสอบ
│   ├── sales_sample.csv
│   └── students_scores.csv
├── src/                    # Source code หลัก
│   ├── config.py           # การตั้งค่า Configuration
│   ├── tools/              # เก็บฟังก์ชัน Tools (เครื่องมือ)
│   │   └── csv_tools.py
│   ├── prompts/            # เก็บ Template คำสั่ง (Prompts)
│   │   └── analysis_prompts.py
│   ├── resources/          # เก็บแหล่งข้อมูล (Resources)
│   │   └── system_resources.py
│   └── utils/              # ฟังก์ชันช่วยเหลือต่างๆ
│       └── validation.py
├── server.py               # จุดเริ่มต้นการทำงาน (Entry Point)
├── .env                    # ไฟล์เก็บ Environment Variables
├── pyproject.toml          # ไฟล์จัดการ Dependencies (ใช้ uv)
└── README.md               # เอกสารประกอบโปรเจ็กต์
```

#### 1.2 สร้างโปรเจ็กต์ใหม่และ Initialize uv
ทำการ Initialize uv ในโฟลเดอร์โปรเจกต์
```bash
uv init --python 3.13 workshop4-mcp-csv-agent
cd workshop4-mcp-csv-agent
```
คำสั่งนี้จะสร้างไฟล์:
- `main.py` (สามารถลบได้)
- `pyproject.toml` (ใช้สำหรับจัดการ dependencies และการตั้งค่าโปรเจกต์)
- `.python-version` (ระบุเวอร์ชันของ Python ที่ใช้ในโปรเจกต์)
- `.gitignore` (ใช้สำหรับระบุไฟล์หรือโฟลเดอร์ที่ไม่ต้องการให้ Git ติดตาม)
- `README.md` (ไฟล์เอกสารสำหรับโปรเจกต์)
รู้จักกับไฟล์สำคัญ:
- `pyproject.toml`: ไฟล์นี้ใช้สำหรับจัดการ dependencies และการตั้งค่าโปรเจกต์
```toml
[project]
name = "workshop4-mcp-csv-agent"
version = "0.1.0"
description = "MCP Server for CSV Analytics"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
```
- `uv.lock`: ไฟล์ล็อกที่บันทึกเวอร์ชันของแพ็กเกจที่ติดตั้งในโปรเจกต์ เพื่อให้แน่ใจว่าโปรเจกต์จะใช้แพ็กเกจเวอร์ชันเดียวกันทุกครั้งที่ติดตั้ง

ทดสอบติดตั้ง Virtual Environment สำเร็จหรือไม่:
```bash
uv run python --version
```
คำสั่งนี้จะสร้าง Virtual Environment และรัน Python ภายในสภาพแวดล้อมนั้น

#### 1.3 ติดตั้ง FastMCP และ Librayry ที่จำเป็น
ทำการติดตั้ง FastMCP และไลบรารีที่จำเป็นผ่าน uv
```bash
uv add fastmcp pandas python-dotenv
```
อธิบาย:
- `fastmcp`: ไลบรารีหลักสำหรับสร้าง MCP Server และ Client
- `pandas`: ไลบรารีสำหรับการจัดการและวิเคราะห์ข้อมูลในรูปแบบตาราง (DataFrame)
- `python-dotenv`: ไลบรารีสำหรับการจัดการ environment variables จากไฟล์ `.env`

#### 1.4 ยืนยันการติดตั้ง FastMCP
หลังจากติดตั้ง FastMCP แล้ว ให้ยืนยันว่าแพ็กเกจถูกติดตั้งเรียบร้อยแล้วโดยใช้คำสั่ง:
```bash
uv pip tree
```
ถ้าเห็นรายการแพ็กเกจที่รวมถึง `fastmcp` แสดงว่าการติดตั้งสำเร็จแล้ว

#### 1.5 เตรียมไฟล์ CSV ตัวอย่างในโปรเจ็กต์
สร้างโฟลเดอร์ `data` ในโปรเจ็กต์ของคุณ และเพิ่มไฟล์ CSV ตัวอย่าง เช่น `sales_sample.csv` และ `students_scores.csv`

#### 1.6 สร้าง validation.py สำหรับตรวจสอบความถูกต้องของไฟล์ CSV
สร้างโฟลเดอร์ `src/utils` และสร้างไฟล์ `validation.py` ภายในโฟลเดอร์นั้น จากนั้นเพิ่มโค้ดต่อไปนี้ลงไป:
```python
import pandas as pd
import os
from pathlib import Path

# --- 1. การหา Path แบบ Robust (ไม่สนว่ารันจากไหน) ---
# โครงสร้าง: my-mcp-agent/src/utils/validation.py
# .parents[0] = utils
# .parents[1] = src
# .parents[2] = my-mcp-agent (Project Root)
PROJECT_ROOT = Path(__file__).resolve().parents[2]

# กำหนด DATA_DIR จาก Root เสมอ
DATA_DIR = PROJECT_ROOT / "data"

# --- Helper Function: ป้องกัน Path Traversal ---
def get_safe_path(filename: str) -> Path:
    """ตรวจสอบความปลอดภัยของ Path และคืนค่า Absolute Path"""
    
    # ถ้าโฟลเดอร์ data ยังไม่มี ให้สร้างรอไว้เลย (Optional)
    if not DATA_DIR.exists():
        os.makedirs(DATA_DIR, exist_ok=True)

    safe_path = (DATA_DIR / filename).resolve()
    
    # ตรวจสอบว่า path ที่ได้ ยังคงอยู่ภายใต้ DATA_DIR หรือไม่
    if not str(safe_path).startswith(str(DATA_DIR.resolve())):
        raise ValueError(f"Access denied: ไม่สามารถเข้าถึงไฟล์นอกโฟลเดอร์ data ได้ ({filename})")
    
    if not safe_path.exists():
        # แก้ไขชื่อตัวแปรให้ตรงกับ parameter (filename)
        raise FileNotFoundError(f"ไม่พบไฟล์ {filename} ในโฟลเดอร์ data") 
        
    return safe_path


# --- Helper Function: แปลง NaN เป็น None ---
def clean_nans(data):
    """Helper: แปลง NaN เป็น None เพื่อให้ JSON Compatible"""
    if isinstance(data, (pd.DataFrame, pd.Series)):
        return data.where(pd.notnull(data), None)
    return data
```

#### 1.7 สร้าง csv_tools.py สำหรับจัดการ CSV Tools
สร้างโฟลเดอร์ `src/tools` และสร้างไฟล์ `csv_tools.py` ภายในโฟลเดอร์นั้น จากนั้นเพิ่มโค้ดต่อไปนี้ลงไป:
```python
from fastmcp import FastMCP
import pandas as pd
from typing import Optional
from src.utils.validation import get_safe_path, clean_nans, DATA_DIR

def register_csv_tools(mcp: FastMCP):

    # Tool to list all CSV files in the data directory
    @mcp.tool()
    def list_csv_files(
        sessionId: Optional[str] = None, 
        action: Optional[str] = None, 
        chatInput: Optional[str] = None, 
        toolCallId: Optional[str] = None) -> list[str]:
        """
        แสดงรายชื่อไฟล์ CSV ที่พร้อมใช้งานใน data
        """
        # ตรวจสอบว่าโฟลเดอร์มีอยู่จริงก่อน glob
        if not DATA_DIR.exists():
            return []
        return [p.name for p in DATA_DIR.glob("*.csv")]


    # Tool to summarize a specific CSV file
    @mcp.tool()
    def summarize_csv(
        file_name: str, 
        sessionId: Optional[str] = None, 
        action: Optional[str] = None, 
        chatInput: Optional[str] = None, 
        toolCallId: Optional[str] = None) -> dict:
        """
        สรุปข้อมูลไฟล์ CSV (จำนวนแถว, คอลัมน์, ตัวอย่างหัวตาราง, สถิติรวมของคอลัมน์ตัวเลข)
        """
        safe_path = get_safe_path(file_name)
        df = pd.read_csv(safe_path)

        summary = {
            "num_rows": len(df),
            "num_columns": len(df.columns),
            "columns": df.columns.tolist(),
            "head": clean_nans(df.head().to_dict(orient="records")),
            "describe": clean_nans(df.describe().to_dict())
        }
        return summary


    # Tool to read rows with pagination
    @mcp.tool()
    def read_csv_rows(
        file_name: str, 
        offset: int = 0, 
        limit: int = 10,
        sessionId: Optional[str] = None, 
        action: Optional[str] = None, 
        chatInput: Optional[str] = None, 
        toolCallId: Optional[str] = None) -> list[dict]:
        """
        อ่านข้อมูลจากไฟล์ CSV แบบระบุจำนวนแถว (Pagination)
        """
        file_path = get_safe_path(file_name)
        df = pd.read_csv(file_path)
        
        if offset >= len(df):
            return []
        
        # Clean NaN ก่อนส่งกลับ
        result_df = clean_nans(df.iloc[offset : offset + limit])
        return result_df.to_dict(orient="records")


    # Tool to get unique values
    @mcp.tool()
    def get_unique_values(
        file_name: str, 
        column_name: str, 
        sessionId: Optional[str] = None, 
        action: Optional[str] = None, 
        chatInput: Optional[str] = None, 
        toolCallId: Optional[str] = None) -> list:
        """
        ดึงค่าที่ไม่ซ้ำกัน (Unique values) จากคอลัมน์ที่ระบุ
        """
        file_path = get_safe_path(file_name)
        df = pd.read_csv(file_path)
        
        if column_name not in df.columns:
            raise ValueError(f"ไม่พบคอลัมน์ {column_name} ในไฟล์ {file_name}")
        
        # dropna() เพื่อไม่ส่งค่าว่างกลับไป
        values = df[column_name].dropna().unique().tolist()
        return values
        
    
    # Tool to filter data using pandas query
    @mcp.tool()
    def filter_csv(
        file_name: str, 
        query: str, 
        sessionId: Optional[str] = None, 
        action: Optional[str] = None, 
        chatInput: Optional[str] = None, 
        toolCallId: Optional[str] = None) -> list[dict]:
        """
        กรองข้อมูล CSV ด้วย query string (ใช้ syntax ของ pandas query)
        
        ตัวอย่างการใช้งาน:
        - "age > 25"
        - "department == 'Sales' and salary > 50000"
        - "product_name.str.contains('Pro', case=False)"
        """
        file_path = get_safe_path(file_name)
        df = pd.read_csv(file_path)
        
        try:
            result = df.query(query, engine='python')
            return clean_nans(result).to_dict(orient="records")
        except Exception as e:
            raise ValueError(f"Query Error: {str(e)}")
```

#### 1.8 สร้าง analysis_prompts.py สำหรับเก็บ Prompt Templates
สร้างโฟลเดอร์ `src/prompts` และสร้างไฟล์ `analysis_prompts.py` ภายในโฟลเดอร์นั้น จากนั้นเพิ่มโค้ดต่อไปนี้ลงไป:
```python
from fastmcp import FastMCP

def register_prompts(mcp: FastMCP):
    
    @mcp.prompt()
    def analyze_monthly_report(filename: str) -> str:
        """แม่แบบสำหรับสั่งให้ AI วิเคราะห์รายงานประจำเดือน"""
        return f"""
        คุณคือผู้เชี่ยวชาญด้าน Data Analyst
        1. กรุณาอ่านไฟล์ CSV ชื่อ: {filename}
        2. สรุปยอดขายรวม และแนวโน้มที่น่าสนใจ
        3. หาความผิดปกติของข้อมูล (Anomalies)
        4. ตอบกลับในรูปแบบ Markdown Table
        """

    @mcp.prompt()
    def debug_csv_format() -> str:
        """แม่แบบสำหรับช่วยแก้ปัญหาไฟล์ CSV"""
        return "ช่วยตรวจสอบโครงสร้างไฟล์ CSV นี้หน่อยว่าถูกต้องตามมาตรฐานหรือไม่ และมีคอลัมน์ไหนหายไปบ้าง"
```

#### 1.9 สร้าง system_resources.py สำหรับเก็บ Resources
สร้างโฟลเดอร์ `src/resources` และสร้างไฟล์ `system_resources.py` ภายในโฟลเดอร์นั้น จากนั้นเพิ่มโค้ดต่อไปนี้ลงไป:
```python
from fastmcp import FastMCP
from src.utils.validation import DATA_DIR

def register_resources(mcp: FastMCP):

    # Resource แบบ Static (ค่าคงที่)
    @mcp.resource("config://app-version")
    def get_app_version() -> str:
        return "CSV Agent v1.0.0 (Beta)"

    # Resource แบบ Dynamic (อ่านจากไฟล์จริง)
    @mcp.resource("csv://schema/{filename}")
    def get_csv_schema(filename: str) -> str:
        """อ่านแค่บรรทัดแรก (Header) ของไฟล์ CSV เพื่อดู Schema"""
        # (ควรใส่ logic get_safe_path ที่นี่ด้วย)
        file_path = DATA_DIR / filename
        if file_path.exists():
             with open(file_path, "r", encoding="utf-8") as f:
                 return f.readline().strip()
        return "Error: File not found"
```

#### 1.10 สร้างไฟล์ server.py สำหรับรัน MCP Server
สร้างไฟล์ `server.py` ในโฟลเดอร์โปรเจ็กต์ของคุณ และเพิ่มโค้ดต่อไปนี้ลงไป:
```python
from fastmcp import FastMCP
from src.tools.csv_tools import register_csv_tools
from src.prompts.analysis_prompts import register_prompts
from src.resources.system_resources import register_resources

# 1. Initialize
mcp = FastMCP("mcp-csv-agent")

# 2. Register Tools (ส่ง mcp ไปให้ csv_tools ช่วยแปะ)
register_csv_tools(mcp)

# 3. Register Prompts
register_prompts(mcp)

# 4. Register Resources
register_resources(mcp)

# 5. Run
if __name__ == "__main__":
    print("🚀 Server starting with Tools, Prompts, and Resources loaded!")
    mcp.run(transport="http", host="0.0.0.0", port=8000)
```

#### 1.11 รัน MCP Server
รัน MCP Server โดยใช้คำสั่ง:
```bash
uv run python server.py
```
Server จะรันด้วย HTTP Transport ที่ port 8000 (http://127.0.0.1:8000/mcp)

#### 1.12 เครื่องมือ (Tools) ที่มีให้ใช้งาน
| Tool Name | รายละเอียด |
|-----------|------------|
| `list_csv_files` | แสดงรายชื่อไฟล์ CSV ทั้งหมดที่มีในโฟลเดอร์ `data` |
| `summarize_csv` | สรุปข้อมูลไฟล์ CSV (จำนวนแถว, คอลัมน์, ตัวอย่างหัวตาราง, สถิติรวมของคอลัมน์ตัวเลข) |
| `read_csv_rows` | อ่านข้อมูลจากไฟล์ CSV แบบระบุจำนวนแถว (Pagination) |
| `get_unique_values` | ดึงค่าที่ไม่ซ้ำกัน (Unique values) จากคอลัมน์ที่ระบุ |
| `filter_csv` | กรองข้อมูล CSV ด้วย query string (ใช้ syntax ของ pandas query) |

#### 1.13 ความสามารถ (Features)

#### 1.13.1 Tools (เครื่องมือ)
AI สามารถเรียกใช้ฟังก์ชันเหล่านี้ได้:
- **`list_csv_files`**: แสดงรายชื่อไฟล์ CSV ทั้งหมดที่มีในโฟลเดอร์ `data/`
- **`summarize_csv`**: สรุปข้อมูลเบื้องต้นของไฟล์ CSV (จำนวนแถว, คอลัมน์, ตัวอย่างข้อมูล, สถิติทางคณิตศาสตร์)
- **`read_csv_rows`**: อ่านข้อมูลจากไฟล์ CSV แบบระบุจำนวนแถว (Pagination)
- **`get_unique_values`**: ดึงค่าที่ไม่ซ้ำกันจากคอลัมน์ที่ระบุ
- **`filter_csv`**: กรองข้อมูลด้วย Query String (เช่น `age > 25`)

#### 1.13.2 Prompts (คำสั่งสำเร็จรูป)
Template คำสั่งที่ช่วยให้ผู้ใช้เริ่มงานได้เร็วขึ้น:
- **`analyze_monthly_report`**: สั่งให้ AI วิเคราะห์ไฟล์ CSV เพื่อสรุปยอดขายและหาความผิดปกติ
- **`debug_csv_format`**: ให้ AI ช่วยตรวจสอบโครงสร้างไฟล์ CSV

#### 1.13.3 Resources (แหล่งข้อมูล)
ข้อมูลที่ AI สามารถเข้าถึงได้โดยตรง:
- **`config://app-version`**: ตรวจสอบเวอร์ชันของ Agent
- **`csv://schema/{filename}`**: อ่านบรรทัดแรก (Header) ของไฟล์ CSV เพื่อดูชื่อคอลัมน์

#### 1.14 ตัวอย่างการใช้งาน (Example Usage)

เมื่อเชื่อมต่อกับ MCP Client (เช่น Claude Desktop) คุณสามารถลองใช้คำสั่งเหล่านี้:

#### 1.14.1 ทดสอบ Tools
- "ช่วยลิสต์ไฟล์ CSV ที่มีทั้งหมดให้หน่อย" (เรียก `list_csv_files`)
- "สรุปข้อมูลจากไฟล์ sales_sample.csv ให้ที" (เรียก `summarize_csv`)
- "ขอดูยอดขายของภาคเหนือ (Region = 'North') จากไฟล์ sales_sample.csv" (เรียก `filter_csv`)
- "ในไฟล์ sales_sample.csv มีสินค้าอะไรขายบ้าง ขอแบบไม่ซ้ำกัน" (เรียก `get_unique_values`)

#### 1.14.2 ทดสอบ Prompts
- เลือก Prompt: **`analyze_monthly_report`** แล้วใส่ค่า `sales_sample.csv`
  - *ผลลัพธ์:* AI จะอ่านไฟล์และวิเคราะห์ยอดขายให้อัตโนมัติ
- เลือก Prompt: **`debug_csv_format`**
  - *ผลลัพธ์:* AI จะเตรียมพร้อมสำหรับการตรวจสอบโครงสร้างไฟล์

#### 1.14.3 ทดสอบ Resources
- "ขอดูเวอร์ชันของ App นี้หน่อย" (อ่าน `config://app-version`)
- "ขอ Schema ของไฟล์ students_scores.csv" (อ่าน `csv://schema/students_scores.csv`)

#### 1.15 ตั้งค่าเชื่อมต่อกับ MCP Server แบบ HTTP ใน Claude Desktop
1. เปิด Claude Desktop
2. ไปที่ Settings > Developer > Local MCP servers
3. คลิก "Edit Config"
4. เพิ่มการเชื่อมต่อใหม่โดยตั้งค่าดังนี้:
    ```json
    {
        "mcpServers": {
            "CSV Analytics Tools": {
                "command": "npx",
                "args": [   
                    "-y",
                    "mcp-remote",
                    "http://127.0.0.1:8000/mcp",
                    "--allow-http"
                ]
            }
        }
    ```
   - Name: `CSV Analytics Tools`
   - URL: `http://localhost:8000/mcp`
5. บันทึกการตั้งค่า

#### 1.16 ตั้งค่าเชื่อมต่อ VSCode กับ MCP Server แบบ HTTP
1. แก้ไขไฟล์ `.vscode/mcp.json` และเพิ่มโค้ดต่อไปนี้ลงไป:
```json
{
    "servers": {
        "CSV Analytics Tools": {
            "command": "npx",
            "args": [
                "-y",
                "mcp-remote",
                "http://127.0.0.1:8000/mcp"
            ]
        }
    }
}
```
2. เปิดไฟล์ที่ต้องการใช้ MCP Agent ใน VSCode
3. กด `Ctrl+Shift+P` (หรือ `Cmd+Shift+P` บน Mac) เพื่อเปิด Command Palette
4. พิมพ์ `Claude: Connect to MCP Server` แล้วเลือกการเชื่อมต่อที่สร้างขึ้น (CSV Analytics Tools)
5. เริ่มแชทกับ MCP Agent ได้เลย!

---

### 2. Deploy MCP Server to FastMCP Cloud
ในส่วนนี้เราจะทำการดีพลอย MCP Server ที่เราได้สร้างขึ้นไปยัง FastMCP Cloud เพื่อให้สามารถเข้าถึงได้จากทุกที่ผ่านอินเทอร์เน็ต
#### 2.1 เตรียมโปรเจ็กต์สำหรับดีพลอย
ตรวจสอบให้แน่ใจว่าโปรเจ็กต์ของคุณมีไฟล์ `server.py` ที่พร้อมรัน MCP Server แล้ว

#### 2.2 นำโปรเจ็กต์ขึ้น GitHub
สร้าง Repository ใหม่บน GitHub และอัปโหลดโค้ดโปรเจ็กต์ของคุณไปยัง Repository นั้น
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin <YOUR_GITHUB_REPO_URL>
git branch -M main
git push -u origin main
```

#### 2.3 ดีพลอยไปยัง FastMCP Cloud
1. ไปที่ [FastMCP Cloud](https://fastmcp.cloud/)
2. ลงชื่อหรือสร้างบัญชีผู้ใช้ใหม่
3. คลิกที่ "+ New Server" เลือก "Hosted"
4. เชื่อมต่อกับ GitHub Repository ของคุณ
5. กำหนดค่า Environment Variables (ถ้ามี)
6. คลิก "Deploy" เพื่อเริ่มกระบวนการดีพลอย

#### 2.4 ตรวจสอบสถานะการดีพลอย
หลังจากคลิก "Deploy" ระบบจะเริ่มกระบวนการดีพลอย คุณสามารถตรวจสอบสถานะได้จากแดชบอร์ดของ FastMCP Cloud เมื่อการดีพลอยเสร็จสมบูรณ์ คุณจะได้รับ URL สำหรับเข้าถึง MCP Server ของคุณผ่านอินเทอร์เน็ต

---
### 3. N8N Integration with MCP Server
ในส่วนนี้เราจะทำการเชื่อมต่อ MCP Server ที่เราได้ดีพลอยไปยัง FastMCP Cloud กับ N8N เพื่อให้สามารถใช้งาน MCP Tools ผ่าน Workflow

#### 3.1 สมัครอีเมล์ฟรีกันก่อนที่เว็บไซต์ mail.tm/th
1. ไปที่ [mail.tm/th](https://mail.tm/th)
2. คลิก "สร้างบัญชีใหม่"
3. กรอกข้อมูลที่จำเป็นและทำตามขั้นตอนจนเสร็จ
4. เมื่อสมัครเสร็จแล้ว ให้เข้าสู่ระบบและคัดลอกอีเมล์และรหัสผ่านไว้ใช้ในขั้นตอนถัดไป

#### 3.2 สมัคร N8N Cloud
1. ไปที่ [N8N Cloud](https://n8n.io)
2. คลิก "Get Started for Free"
3. ลงทะเบียนด้วยอีเมล์ที่สมัครจาก mail.tm/th
4. ยืนยันอีเมล์และเข้าสู่ระบบ N8N Cloud

#### 3.3 สร้าง Workflow ใน N8N
1. หลังจากเข้าสู่ระบบ N8N Cloud ให้คลิก "New Workflow"
2. เพิ่ม Trigger Node "On Chat Message"
3. เพิ่ม Node "AI Agent" และตั้งค่าให้เชื่อมต่อกับ MCP Server ของคุณโดยใช้ URL ที่ได้จาก FastMCP Cloud
4. กำหนดค่า Node "AI Agent" ให้ใช้ Tools จาก MCP Server ของคุณ
5. เชื่อมต่อ Node "On Chat Message" กับ Node "AI Agent"
6. บันทึก Workflow และทดสอบการทำงานโดยส่งข้อความผ่าน Trigger Node
